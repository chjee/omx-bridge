{
  "name": "omx-bridge-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "OpenClaw agent tools for the local omx-bridge service",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@sinclair/typebox": "^0.34.41",
    "openclaw": "*"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  }
}
