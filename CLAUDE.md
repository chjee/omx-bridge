@AGENTS.md

## Claude Code

Claude Code reads `CLAUDE.md`, not `AGENTS.md` directly. Keep this file as the
Claude project entrypoint that imports the shared agent contract above.

Use project files for repository-specific commands, architecture, branches,
deployment steps, and test data. Keep personal, repository-neutral preferences
in `~/.claude/CLAUDE.md`.
